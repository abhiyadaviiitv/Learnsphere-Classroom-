spring:
  application:
    name: user-service
  
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://madaraxghost404:rZd9rwWtNqL7CWVr@classroom.5qglv.mongodb.net/?appName=classroom}
      database: ${MONGODB_DATABASE:classroom}
  
  # OAuth2 Configuration
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: email,profile
            redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/oauth2/callback/google}
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope: user:email,read:user
            redirect-uri: ${GITHUB_REDIRECT_URI:http://localhost:8080/oauth2/callback/github}
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Frontend URL
frontend:
  url: ${FRONTEND_URL:http://localhost:3000}

# JWT Configuration
jwt:
  secret: c0349809771c2601589f51e8ee2d970d04b66d5237cc7c5e
  expiration: ${JWT_EXPIRATION:86400000}

# Server Configuration
server:
  port: 8081

# Eureka Configuration
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.learnsphere.user: DEBUG
    org.springframework.security: INFO



